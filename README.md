# puc-mvp-backend

A simple fullstack application

- Run docker compose up, to download and configure postgres database automatically

- Then run gradle with the following command: ./gradlew bootRun (to use gradle wrapper).

You cann access swagger-ui using the following url: [Swagger-ui](http://localhost:8080/swagger-ui/index.html#/)

